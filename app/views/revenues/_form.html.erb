<div class="card-body">

  <%= form_with(model: revenue, local: true) do |form| %>
    <% if revenue.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(revenue.errors.count, "error") %> prohibited this revenue from being saved:</h2>

        <ul>
          <% revenue.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group row">
      <label class="col-lg-3 control-label text-lg-right pt-2" for="inputDefault">Description</label>
      <div class="col-lg-6">
        <%= form.text_field :name, :class => 'form-control', :placeholder => "Enter First Description", required: true %>
      </div>
    </div>

    <div class="form-group row">
      <label class="col-lg-3 control-label text-lg-right pt-2" for="inputDefault">Amount</label>
      <div class="col-lg-6">
        <%= form.number_field :amount, :class => 'form-control', :placeholder => "Enter First Amount", required: true %>
      </div>
    </div>

    <div class="actions">
      <%= form.submit class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>